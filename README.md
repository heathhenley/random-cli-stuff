### Random one-off scripts
Adding some random one-offs or helper / util scripts as they come up


### Running
Use `uv` (why would you use anything else at this point? 😀)
```
uv run main.py --help
```